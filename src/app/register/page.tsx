import React from 'react';
import Layout from '../layout';

const RegisterPage: React.FC = () => {
    return (
        <Layout>
            <h1 className="text-center mt-10 text-2xl">Ini adalah Register Page</h1>
        </Layout>
    );
};

export default RegisterPage;
